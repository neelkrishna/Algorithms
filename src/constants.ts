export const graph: Map<string, string[]> = new Map<string, string[]>();
graph.set("a", ["b", "c"]);
graph.set("b", ["d"]);
graph.set("c", ["e"]);
graph.set("d", ["f"]);
graph.set("e", []);
graph.set("f", []);

